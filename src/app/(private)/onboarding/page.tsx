import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/ui';

export default function OnboardingPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>
          Welcome
        </CardTitle>
        <CardDescription>
          First, thing first, we need to set you up
        </CardDescription>
      </CardHeader>
      <CardContent></CardContent>
    </Card>
  );
}
